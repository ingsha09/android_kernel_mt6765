name: Manual Kernel Build (MT6765)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Install Build Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential bc libncurses5-dev libssl-dev \
            bison flex libelf-dev clang lld llvm ccache zip unzip curl git \
            gcc-aarch64-linux-gnu gcc-arm-linux-gnueabi python3 python-is-python3

      - name: Cache Compiler & Toolchain
        id: cache-toolchain
        uses: actions/cache@v4
        with:
          path: |
            ~/.cache/ccache
            ${{ github.workspace }}/toolchain
          key: ${{ runner.os }}-proton-clang-v1
          restore-keys: |
            ${{ runner.os }}-proton-clang-

      - name: Setup Compiler (Proton Clang)
        run: |
          if [ ! -d "${{ github.workspace }}/toolchain/bin" ]; then
            echo "Toolchain cache miss. Cloning Proton Clang..."
            mkdir -p ${{ github.workspace }}/toolchain
            git clone --depth=1 https://github.com/kdrag0n/proton-clang.git ${{ github.workspace }}/toolchain
          else
            echo "Toolchain found in GitHub Cache!"
          fi

      - name: Fix MTK drvgen.mk DTBO Crash
        run: |
          # Prevent drvgen.mk from crashing when dtboimg.cfg.tmp doesn't exist
          if [ -f "scripts/drvgen/drvgen.mk" ]; then
            sed -i 's/if ! cmp -s dtboimg.cfg.tmp dtboimg.cfg;/if true;/g' scripts/drvgen/drvgen.mk
            sed -i 's/if ! cmp -s dtbimg.cfg.tmp dtbimg.cfg;/if true;/g' scripts/drvgen/drvgen.mk
          fi

      - name: Integrate KernelSU & SUSFS Source Patches
        run: |
          curl -LSs "https://raw.githubusercontent.com/tiann/KernelSU/main/kernel/setup.sh" | bash -
          
          if [ ! -d "${{ github.workspace }}/susfs" ]; then
            git clone --depth=1 https://gitlab.com/simonpunk/SUSFS4KSU.git ${{ github.workspace }}/susfs
          fi
          
          cp -r ${{ github.workspace }}/susfs/kernel_patches/fs/* fs/
          cp -r ${{ github.workspace }}/susfs/kernel_patches/include/linux/* include/linux/

      - name: Build Kernel
        run: |
          export PATH="${{ github.workspace }}/toolchain/bin:$PATH"
          export ARCH=arm64
          export SUBARCH=arm64
          export CC="ccache clang"
          export CXX="ccache clang++"
          export CROSS_COMPILE=aarch64-linux-gnu-
          export CROSS_COMPILE_ARM32=arm-linux-gnueabi-
          export LLVM=1
          export LLVM_IAS=1

          ccache -M 5G
          ccache -z

          mkdir -p out
          # Pre-create empty config files to prevent build system crashes
          touch out/dtboimg.cfg out/dtbimg.cfg out/dtboimg.cfg.tmp out/dtbimg.cfg.tmp

          make ARCH=arm64 O=out blossom_defconfig
          make -j$(nproc --all) ARCH=arm64 O=out

          ccache -s

      - name: Package with AnyKernel3
        run: |
          git clone --depth=1 https://github.com/osm0sis/AnyKernel3.git AK3
          cp out/arch/arm64/boot/Image.gz-dtb AK3/ 2>/dev/null || cp out/arch/arm64/boot/Image AK3/
          
          sed -i 's/device.name1=/device.name1=dandelion/g' AK3/anykernel.sh
          sed -i 's/device.name2=/device.name2=blossom/g' AK3/anykernel.sh
          
          cd AK3
          zip -r9 ../blossom-ksu-susfs-kernel.zip * -x .git README.md *placeholder

      - name: Upload Flashable Kernel ZIP
        uses: actions/upload-artifact@v4
        with:
          name: blossom-ksu-susfs-kernel
          path: blossom-ksu-susfs-kernel.zip
